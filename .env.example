# Optional: AI educational explainer (never send raw DNA — only PRS summaries)
OPENAI_API_KEY=sk-...
# OPENAI_MODEL=gpt-4o-mini
# OPENAI_BASE_URL=https://api.openai.com/v1

# GeneScope API (account + longitudinal storage mode)
NEXT_PUBLIC_API_URL=http://localhost:8000
